<!DOCTYPE html>
<html lang="en">
<head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" href="/style.css">
        <title>All Pots</title>
</head>
<body>
        <h1>Qura Posts</h1>
        <% for (let i = 0; i < posts.length; i++) { %>
                <div class="post">
                        <h3 class="user">@<%= posts[i].username %></h3>
                        <h4 class="content"><%= posts[i].content %></h4>
                        <button><a href="http://localhost:8080/posts/<%= posts[i].id %>">See in detail</a></button>
                        <button><a href="http://localhost:8080/posts/<%= posts[i].id %>/edit">Update</a></button>
                        <form action="/posts/<%= posts[i].id %>?_method=DELETE" method="post">
                                <button>Delete</button>
                        </form>
                </div>
        <% } %>

        <br><br>

        <button><a href="http://localhost:8080/posts/new">Create New Post</a></button>
</body>
</html>