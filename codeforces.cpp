#include <iostream>
#include <vector>
using namespace std;

int main()
{
        int t;
        cin >> t;
        for (int s = 1; s <= t; s++)
        {
                int n;
                vector <int>v;
                cin >> n;
                for (int i = 0; i < n; i++)
                {
                        int m;
                        cin >> m;
                        v.push_back(m);
                }
                int us=max()
        }
}